前回の調査結果に基づいて、最小変更で実装してください。

作成・編集対象:
- `src/descriptor_all2_R.cpp`
- `build/build_descriptor2_R.py`

要件:
1. `descriptor_all_R.cpp` をもとに `descriptor_all2_R.cpp` を作る
2. モジュール名を `descriptor_all2_R` にする
3. pybind API に `coef_py` を追加する
4. 係数長と内部特徴量次元を照合し、不一致なら明確にエラーを出す
5. force feature の full `(3N, N_feat)` を実体化しない
6. `coef[kfeat]` との縮約を、前回特定した最も早い安全な箇所で融合する
7. 必要なら `lambda_compact` のような縮約済みバッファを使う
8. 最終 force はできれば `(N,3)` で返す
9. OpenMP 構造は維持する
10. 元の `descriptor_all_R.cpp` は変更しないか、変更が必要でも最小限にする

重要:
- 参照パスのズレがある場合は、必要最小限だけ修正する
- 旧推論との比較可能性を壊さない
- ファイル全体の全面書き換えは避ける
- 主目的は推論高速化です。full `(3N, N_feat)` の回避、早期縮約、不要な一時配列削減を意識してください。

最後に報告:
- 実際に変更したループ
- 係数縮約を入れた位置
- 必要だった最小限のパス修正
- API 変更点